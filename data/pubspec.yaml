name: data
publish_to: none

environment:
  sdk: '>=2.19.0'

dependencies:
  core:
    path: ../core
  domain:
    path: ../domain
  flutter:
    sdk: flutter
  # For Architecture
  flutter_riverpod:
  riverpod_annotation:
  # Dependency Injection
  injectable:
  get_it:
  # For Networking
  dio:
  http:
  # For Model
  freezed_annotation:
  shared_preferences:

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner:
  # For Architecture
  riverpod_generator:
  riverpod_lint:
  # Dependency Injection
  injectable_generator:
  # For Model
  freezed:
  json_serializable:
  # For Analyzer
  custom_lint:
  flutter_lints:
  # For Testing
  mocktail:
